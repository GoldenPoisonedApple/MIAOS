# 分散タスク処理システム 設計書

## 1. システム全体像

本システムは、複数台のPCリソースを有効活用して機械学習やセキュリティ評価のプログラム（mia_attack 等）を効率的に並列実行するための「マスター・ワーカー型」分散処理基盤です。

Celery + Redisをベースとし、共有ストレージへのアクセス制限やファイル競合問題を解決するために、ファイル入出力をMinIO経由で行うアーキテクチャを採用しています。

## 2. ノード構成と役割

### 2.1 マスターノード (ksl-v03: 133.44.75.128)
タスクの管理とデータの集約を担う専用のVM（仮想マシン）サーバーです。
* **Redis (キュー)**: タスクの実行条件と、ワーカーのキュー待ち行列を管理します。
* **MinIO (オブジェクトストレージ)**: `kslib` (NAS) をバックエンドの保存先としてマウントし、画像やモデルファイルなどの大きなバイナリデータを保存・配信します。（S3互換APIとして直接ワーカーから通信）
* **PostgreSQL (リレーショナルデータベース)**: 実行ごとのハイパーパラメータ、精度（メトリクス）、実行時間、ステータス、MinIO上のファイルパスなど、構造化されたデータを保存します。
* **Tracking API (Rust/Axum等のバックエンド)**: ワーカーから実行メタデータ（JSON）を受け取り、PostgreSQLに保存するためのAPIサーバー。DBとの直接の密結合を防ぎます。
* **可視化・管理サーバー (将来構築)**: タスクの投入や、情報を可視化するダッシュボード。（Tracking APIを拡張して実装可能）

### 2.2 ワーカーノード (nn01, nn02, nn11, t43)
タスクを取得し、実際の演算処理を行うPC群です。
* **Celeryワーカー (Dockerコンテナ)**:
  * 環境構築済みのDockerコンテナ内で起動しっぱなし（常駐）にします。
  * 本部(ksl-v03)のRedisを常に監視し、空きがあればタスクを取得します。
  * 実行時は、タスクごとにローカルディスク（`/tmp/task_<UUID>`など）に一時ディレクトリを作成し、共有ストレージ(NAS)上でのファイル競合を防ぎます。

## 3. タスク実行のワークフロー

1. **タスク投入**: ユーザーまたは管理スクリプトが、実行条件（ハイパーパラメータ等）をksl-v03のRedisキューに登録する。
2. **タスク取得**: 待機中のワーカー（Celery）がRedisから条件を取得する。
3. **環境準備 (キャッシュ機能)**: 
   * ワーカーはローカルに一時実行ディレクトリを作成する。
   * 必要なデータセットやモデルがローカルキャッシュにない、またはハッシュが古い場合は、ksl-v03のMinIOからダウンロードする。
4. **プログラム実行**: コンテナ内の同一Python環境下で、`main.py` に相当する処理を呼び出して実行する。
5. **結果保存**:
   * **成功時**: 実行結果（約25MB）をksl-v03のMinIOへアップロードし、ステータスを「完了」とする。
   * **エラー時**: 実行結果データは破棄し、スタックトレースやPC名などをエラーステータスとして記録する。
6. **後処理**: ローカルの一時実行ディレクトリを削除し、次のタスクを待機する。

## 4. 懸念事項の解決策（振り返り）
* **NASでのファイル衝突**: 実行時のファイル書き出しを各PCのローカルディスクに行うことで解決。
* **ネットワークアクセス制限**: `kslib` にアクセスできないPCも、ksl-v03に立てたMinIO(HTTP)を経由することでアクセス可能に。
* **モデルのアーキテクチャ互換性 (arm64/amd64)**: Dockerfileを共通化し、PyTorchのモデル保存・読み込みを `state_dict` 形式に統一することで解決。
* **帯域・ストレージ圧迫**: 各ワーカーにデータキャッシュの仕組みを実装することで、無駄なダウンロードを防止。

---

## 5. 今後すべきこと（実装ロードマップ）

この設計に基づいて、以下のフェーズで実装を進めていきます。

### Phase 1: マスターノード (ksl-v03) のインフラ構築
- [x] ksl-v03 に Docker, Docker Compose をインストールする。
- [x] ksl-v03 上で `kslib` (NAS) をマウントする。
- [x] ksl-v03 上で `docker-compose.yml` を作成し、**Redis** コンテナを立ち上げる。
- [x] ksl-v03 上で **MinIO** コンテナを立ち上げ、データ保存先を `kslib` のマウントパスに指定する。
- [x] ksl-v03 上で **PostgreSQL** コンテナを立ち上げ、実行結果のメタデータ管理用データベースを用意する。
- [ ] **(New)** Rust (Axum等) を用いてメタデータ受信用の **Tracking API** コンテナを作成・デプロイする。

### Phase 2: PythonコードのCelery対応（ワーカー実装）
- [ ] 必要なライブラリ（`celery`, `redis`, `boto3` または `minio`等）を `Dockerfile` および `Dockerfile.arm64` に追加する。
- [ ] `tasks.py` (仮) を作成し、Celeryアプリケーションを定義する。
- [ ] 既存の `main.py` を呼び出せるように、Celeryの `@app.task` デコレータをつけたラッパー関数を作成する。

### Phase 3: データ連携とキャッシュ機能の実装
- [ ] タスク開始時に、MinIOから必要なデータをダウンロードする関数を実装する。（ハッシュ比較によるキャッシュロジックも組み込む）
- [ ] タスク終了時に、結果をMinIOへアップロードする関数を実装する。
- [ ] 実行時の一時ディレクトリ作成・削除（クリーンアップ）ロジックを実装する。

### Phase 4: テスト実行とワーカーのデプロイ
- [ ] 1台のPCでワーカーコンテナを起動し、簡単なタスクが成功・失敗（エラーハンドリング）するかテストする。
- [ ] 他のPC (nn01, nn11, t43) でもワーカーコンテナを立ち上げ、並列でタスクを消化できるか確認する。

### Phase 5: 可視化・管理システムの開発 (オプション)
- [ ] MinIOやRedis(または追加したDB)から情報を読み取り、タスクの進捗や結果を確認できるダッシュボードを自作する。



## 自分用メモ
- タスクの取得 ( Celery <-> Redis ): 直接通信。ワーカーが暇になったら自動でRedisからタスクを引く。
- 大容量ファイル ( ワーカー <-> MinIO ): 直接通信。S3互換APIで数十MBのファイルを高速にアップロード/ダウンロード。
- メタデータの保存 ( ワーカー <-> Tracking API(Rust) <-> PostgreSQL ): API経由。ワーカーはRust APIにJSONを投げ、RustがDB（PostgreSQL）に安全に保存する。